import React from 'react'
import { Text } from 'react-native'
import { ScrollView } from 'react-native-gesture-handler'

const Login = () => {
    return (
        <ScrollView>
            <Text>
                Esto es un texto de muestra, es algo increible.
            </Text>
        </ScrollView>
    )
}

export default Login